// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.

package byok

import (
	"fmt"
	"github.com/kenrogers/openrouter-cli/internal/client"
	"github.com/kenrogers/openrouter-cli/internal/flagutil"
	"github.com/kenrogers/openrouter-cli/internal/interactive"
	"github.com/kenrogers/openrouter-cli/internal/output"
	"github.com/kenrogers/openrouter-cli/internal/sdk"
	"github.com/kenrogers/openrouter-cli/internal/sdk/models/operations"
	"github.com/kenrogers/openrouter-cli/internal/usage"
	"github.com/spf13/cobra"
)

var createCmdMeta = []flagutil.FlagMeta{
	{FlagName: "allowed-models", FieldPath: "Body.AllowedModels", Kind: flagutil.FlagKindJSON, Optional: true, Annotations: `json:"allowed_models,omitempty"`, Description: "Optional allowlist of model slugs this credential may be used for. `null` means no restriction."},
	{FlagName: "allowed-user-ids", FieldPath: "Body.AllowedUserIds", Kind: flagutil.FlagKindJSON, Optional: true, Annotations: `json:"allowed_user_ids,omitempty"`, Description: "Optional allowlist of user IDs that may use this credential. `null` means no restriction."},
	{FlagName: "disabled", FieldPath: "Body.Disabled", Kind: flagutil.FlagKindBool, Optional: true, Description: "Whether this credential should be created in a disabled state."},
	{FlagName: "is-fallback", Shorthand: "i", FieldPath: "Body.IsFallback", Kind: flagutil.FlagKindBool, Optional: true, Description: "Whether this credential is treated as a fallback — used only after non-fallback keys for the same provider have been tried."},
	{FlagName: "key", Shorthand: "k", FieldPath: "Body.Key", Kind: flagutil.FlagKindString, Required: true, Description: "The raw provider API key or credential. This value is encrypted at rest and never returned in API responses. [required]"},
	{FlagName: "name", Shorthand: "n", FieldPath: "Body.Name", Kind: flagutil.FlagKindJSON, Optional: true, Annotations: `json:"name,omitempty"`, Description: "Optional human-readable name for the credential."},
	{FlagName: "provider", Shorthand: "p", FieldPath: "Body.Provider", Kind: flagutil.FlagKindEnum, Required: true, EnumValues: []string{"ai21", "aion-labs", "akashml", "alibaba", "amazon-bedrock", "amazon-nova", "ambient", "anthropic", "arcee-ai", "atlas-cloud", "avian", "azure", "baidu", "baseten", "black-forest-labs", "byteplus", "cerebras", "chutes", "cirrascale", "clarifai", "cloudflare", "cohere", "crusoe", "deepinfra", "deepseek", "dekallm", "featherless", "fireworks", "friendli", "gmicloud", "google-ai-studio", "google-vertex", "groq", "hyperbolic", "inception", "inceptron", "inference-net", "infermatic", "inflection", "io-net", "ionstream", "liquid", "mancer", "mara", "minimax", "mistral", "modelrun", "modular", "moonshotai", "morph", "ncompass", "nebius", "nex-agi", "nextbit", "novita", "nvidia", "open-inference", "openai", "parasail", "perceptron", "perplexity", "phala", "poolside", "recraft", "reka", "relace", "sambanova", "seed", "siliconflow", "sourceful", "stepfun", "streamlake", "switchpoint", "together", "upstage", "venice", "wandb", "xai", "xiaomi", "z-ai"}, Description: "The upstream provider this credential authenticates against, as a lowercase slug (e.g. `openai`, `anthropic`, `amazon-bedrock`). (options: ai21, aion-labs, akashml, alibaba, amazon-bedrock, amazon-nova, ambient, anthropic, arcee-ai, atlas-cloud, avian, azure, baidu, baseten, black-forest-labs, byteplus, cerebras, chutes, cirrascale, clarifai, cloudflare, cohere, crusoe, deepinfra, deepseek, dekallm, featherless, fireworks, friendli, gmicloud, google-ai-studio, google-vertex, groq, hyperbolic, inception, inceptron, inference-net, infermatic, inflection, io-net, ionstream, liquid, mancer, mara, minimax, mistral, modelrun, modular, moonshotai, morph, ncompass, nebius, nex-agi, nextbit, novita, nvidia, open-inference, openai, parasail, perceptron, perplexity, phala, poolside, recraft, reka, relace, sambanova, seed, siliconflow, sourceful, stepfun, streamlake, switchpoint, together, upstage, venice, wandb, xai, xiaomi, z-ai) [required]"},
	{FlagName: "workspace-id", Shorthand: "w", FieldPath: "Body.WorkspaceID", Kind: flagutil.FlagKindString, Optional: true, Description: "Optional workspace ID. Defaults to the authenticated entity's default workspace."},
}

// initCreateCmd initializes the create command.
func initCreateCmd(parent *cobra.Command) error {
	var cmd = &cobra.Command{
		Use:     "create",
		Short:   "Create a BYOK provider credential",
		Long:    "Create a new bring-your-own-key (BYOK) provider credential. The raw key is encrypted at rest and never returned in API responses. Defaults to the authenticated entity's default workspace; use the `workspace_id` body field to scope to a different workspace. [Management key](/docs/guides/overview/auth/management-api-keys) required.",
		Example: "  openrouter byok create --key sk-proj-abc123... --provider openai",
		RunE:    runCreateCmd,
	}
	flagutil.RegisterFlags(cmd, createCmdMeta)
	if err := flagutil.ValidateMeta[operations.CreateBYOKKeyRequest](createCmdMeta); err != nil {
		return fmt.Errorf("invalid metadata for create: %w", err)
	}
	cmd.Flags().String("body", "", "Request body as JSON (alternative to individual flags). Can also be provided via stdin.")
	parent.AddCommand(cmd)
	return nil
}

// runCreateCmd executes the create command.
func runCreateCmd(cmd *cobra.Command, args []string) error {
	if usage.UsageRequested(cmd) {
		return usage.EmitSchema(cmd, cmd.OutOrStdout())
	}
	if interactive.ShouldPrompt(cmd, createCmdMeta) {
		if err := interactive.PromptAndSetFlags(cmd, createCmdMeta); err != nil {
			return err
		}
	}
	req, err := flagutil.BuildRequest[operations.CreateBYOKKeyRequest](cmd, createCmdMeta, "Body", "body")
	if err != nil {
		return err
	}
	s, err := client.NewClient(cmd)
	if err != nil {
		return err
	}
	sdkOpts, err := output.PrepareCallOpts(cmd)
	if err != nil {
		return err
	}
	// Dry-run: force skip deserialization so the synthetic empty response
	// does not cause parse failures in typed response handling.
	if client.IsDryRun(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	if err := output.ValidateGlobalServerName(cmd, sdk.ServerList); err != nil {
		return err
	}
	if output.WantsRawJSON(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	res, err := s.Byok.Create(cmd.Context(), *req, sdkOpts...)
	if err != nil {
		return output.Error(cmd, err)
	}

	if err := output.Result(cmd, res); err != nil {
		return err
	}
	return nil
}
