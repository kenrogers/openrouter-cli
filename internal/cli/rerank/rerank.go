// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.

package rerank

import (
	"fmt"
	"github.com/kenrogers/openrouter-cli/internal/client"
	"github.com/kenrogers/openrouter-cli/internal/flagutil"
	"github.com/kenrogers/openrouter-cli/internal/interactive"
	"github.com/kenrogers/openrouter-cli/internal/output"
	"github.com/kenrogers/openrouter-cli/internal/sdk"
	"github.com/kenrogers/openrouter-cli/internal/sdk/models/operations"
	"github.com/kenrogers/openrouter-cli/internal/usage"
	"github.com/spf13/cobra"
)

var rerankCmdMeta = []flagutil.FlagMeta{
	{FlagName: "documents", FieldPath: "Body.Documents", Kind: flagutil.FlagKindStringArray, Required: true, Description: "The list of documents to rerank [required]"},
	{FlagName: "model", Shorthand: "m", FieldPath: "Body.Model", Kind: flagutil.FlagKindString, Required: true, Description: "The rerank model to use [required]"},
	{FlagName: "provider", Shorthand: "p", FieldPath: "Body.Provider", Kind: flagutil.FlagKindJSON, Optional: true, Annotations: `json:"provider,omitempty"`, Description: "JSON object"},
	{FlagName: "query", FieldPath: "Body.Query", Kind: flagutil.FlagKindString, Required: true, Description: "The search query to rerank documents against [required]"},
	{FlagName: "top-n", Shorthand: "t", FieldPath: "Body.TopN", Kind: flagutil.FlagKindInt64, Optional: true, Description: "Number of most relevant documents to return"},
}

// initRerankCmd initializes the rerank command.
func initRerankCmd(parent *cobra.Command) error {
	var cmd = &cobra.Command{
		Use:     "rerank",
		Short:   "Submit a rerank request",
		Long:    "Submits a rerank request to the rerank router",
		Example: "  openrouter rerank rerank --documents '[\"Paris is the capital of France.\",\"Berlin is the capital of Germany.\"]' --model cohere/rerank-v3.5 --query What is the capital of France?",
		RunE:    runRerankCmd,
	}
	flagutil.RegisterFlags(cmd, rerankCmdMeta)
	if err := flagutil.ValidateMeta[operations.CreateRerankRequest](rerankCmdMeta); err != nil {
		return fmt.Errorf("invalid metadata for rerank: %w", err)
	}
	cmd.Flags().String("body", "", "Request body as JSON (alternative to individual flags). Can also be provided via stdin.")
	parent.AddCommand(cmd)
	return nil
}

// runRerankCmd executes the rerank command.
func runRerankCmd(cmd *cobra.Command, args []string) error {
	if usage.UsageRequested(cmd) {
		return usage.EmitSchema(cmd, cmd.OutOrStdout())
	}
	if interactive.ShouldPrompt(cmd, rerankCmdMeta) {
		if err := interactive.PromptAndSetFlags(cmd, rerankCmdMeta); err != nil {
			return err
		}
	}
	req, err := flagutil.BuildRequest[operations.CreateRerankRequest](cmd, rerankCmdMeta, "Body", "body")
	if err != nil {
		return err
	}
	s, err := client.NewClient(cmd)
	if err != nil {
		return err
	}
	sdkOpts, err := output.PrepareCallOpts(cmd)
	if err != nil {
		return err
	}
	// Dry-run: force skip deserialization so the synthetic empty response
	// does not cause parse failures in typed response handling.
	if client.IsDryRun(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	if err := output.ValidateGlobalServerName(cmd, sdk.ServerList); err != nil {
		return err
	}
	if output.WantsRawJSON(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	res, err := s.Rerank.Rerank(cmd.Context(), *req, sdkOpts...)
	if err != nil {
		return output.Error(cmd, err)
	}

	if err := output.Result(cmd, res); err != nil {
		return err
	}
	return nil
}
