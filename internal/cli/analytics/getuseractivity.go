// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.

package analytics

import (
	"fmt"
	"github.com/kenrogers/openrouter-cli/internal/client"
	"github.com/kenrogers/openrouter-cli/internal/flagutil"
	"github.com/kenrogers/openrouter-cli/internal/interactive"
	"github.com/kenrogers/openrouter-cli/internal/output"
	"github.com/kenrogers/openrouter-cli/internal/sdk"
	"github.com/kenrogers/openrouter-cli/internal/sdk/models/operations"
	"github.com/kenrogers/openrouter-cli/internal/usage"
	"github.com/spf13/cobra"
)

var getUserActivityCmdMeta = []flagutil.FlagMeta{
	{FlagName: "date", FieldPath: "Date", Kind: flagutil.FlagKindString, Optional: true, Description: "Filter by a single UTC date in the last 30 days (YYYY-MM-DD format)."},
	{FlagName: "api-key-hash", Shorthand: "a", FieldPath: "APIKeyHash", Kind: flagutil.FlagKindString, Optional: true, Description: "Filter by API key hash (SHA-256 hex string, as returned by the keys API)."},
	{FlagName: "user-id", Shorthand: "u", FieldPath: "UserID", Kind: flagutil.FlagKindString, Optional: true, Description: "Filter by org member user ID. Only applicable for organization accounts."},
}

// initGetUserActivityCmd initializes the get-user-activity command.
func initGetUserActivityCmd(parent *cobra.Command) error {
	var cmd = &cobra.Command{
		Use:     "get-user-activity",
		Short:   "Get user activity grouped by endpoint",
		Long:    "Returns user activity data grouped by endpoint for the last 30 (completed) UTC days. [Management key](/docs/guides/overview/auth/management-api-keys) required.",
		Example: "  openrouter analytics get-user-activity",
		RunE:    runGetUserActivityCmd,
		Aliases: []string{"gua"},
	}
	flagutil.RegisterFlags(cmd, getUserActivityCmdMeta)
	if err := flagutil.ValidateMeta[operations.GetUserActivityRequest](getUserActivityCmdMeta); err != nil {
		return fmt.Errorf("invalid metadata for get-user-activity: %w", err)
	}
	parent.AddCommand(cmd)
	return nil
}

// runGetUserActivityCmd executes the get-user-activity command.
func runGetUserActivityCmd(cmd *cobra.Command, args []string) error {
	if usage.UsageRequested(cmd) {
		return usage.EmitSchema(cmd, cmd.OutOrStdout())
	}
	if interactive.ShouldPrompt(cmd, getUserActivityCmdMeta) {
		if err := interactive.PromptAndSetFlags(cmd, getUserActivityCmdMeta); err != nil {
			return err
		}
	}
	req, err := flagutil.BuildRequest[operations.GetUserActivityRequest](cmd, getUserActivityCmdMeta, "", "")
	if err != nil {
		return err
	}
	s, err := client.NewClient(cmd)
	if err != nil {
		return err
	}
	sdkOpts, err := output.PrepareCallOpts(cmd)
	if err != nil {
		return err
	}
	// Dry-run: force skip deserialization so the synthetic empty response
	// does not cause parse failures in typed response handling.
	if client.IsDryRun(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	if err := output.ValidateGlobalServerName(cmd, sdk.ServerList); err != nil {
		return err
	}
	if output.WantsRawJSON(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	res, err := s.Analytics.GetUserActivity(cmd.Context(), req, sdkOpts...)
	if err != nil {
		return output.Error(cmd, err)
	}

	if err := output.Result(cmd, res); err != nil {
		return err
	}
	return nil
}
