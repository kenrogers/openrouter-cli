// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.

package oauth

import (
	"fmt"
	"github.com/kenrogers/openrouter-cli/internal/client"
	"github.com/kenrogers/openrouter-cli/internal/flagutil"
	"github.com/kenrogers/openrouter-cli/internal/interactive"
	"github.com/kenrogers/openrouter-cli/internal/output"
	"github.com/kenrogers/openrouter-cli/internal/sdk"
	"github.com/kenrogers/openrouter-cli/internal/sdk/models/operations"
	"github.com/kenrogers/openrouter-cli/internal/usage"
	"github.com/spf13/cobra"
)

var exchangeAuthCodeForAPIKeyCmdMeta = []flagutil.FlagMeta{
	{FlagName: "code", FieldPath: "Body.Code", Kind: flagutil.FlagKindString, Required: true, Description: "The authorization code received from the OAuth redirect [required]"},
	{FlagName: "code-challenge-method", FieldPath: "Body.CodeChallengeMethod", Kind: flagutil.FlagKindJSON, Optional: true, Annotations: `json:"code_challenge_method,omitempty"`, Description: "The method used to generate the code challenge (options: S256, plain)"},
	{FlagName: "code-verifier", FieldPath: "Body.CodeVerifier", Kind: flagutil.FlagKindString, Optional: true, Description: "The code verifier if code_challenge was used in the authorization request"},
}

// initExchangeAuthCodeForAPIKeyCmd initializes the exchange-auth-code-for-API-key command.
func initExchangeAuthCodeForAPIKeyCmd(parent *cobra.Command) error {
	var cmd = &cobra.Command{
		Use:     "exchange-auth-code-for-API-key",
		Short:   "Exchange authorization code for API key",
		Long:    "Exchange an authorization code from the PKCE flow for a user-controlled API key",
		Example: "  openrouter O-auth exchange-auth-code-for-API-key --code auth_code_abc123def456",
		RunE:    runExchangeAuthCodeForAPIKeyCmd,
		Aliases: []string{"eacfAk"},
	}
	flagutil.RegisterFlags(cmd, exchangeAuthCodeForAPIKeyCmdMeta)
	if err := flagutil.ValidateMeta[operations.ExchangeAuthCodeForAPIKeyRequest](exchangeAuthCodeForAPIKeyCmdMeta); err != nil {
		return fmt.Errorf("invalid metadata for exchange-auth-code-for-API-key: %w", err)
	}
	cmd.Flags().String("body", "", "Request body as JSON (alternative to individual flags). Can also be provided via stdin.")
	parent.AddCommand(cmd)
	return nil
}

// runExchangeAuthCodeForAPIKeyCmd executes the exchange-auth-code-for-API-key command.
func runExchangeAuthCodeForAPIKeyCmd(cmd *cobra.Command, args []string) error {
	if usage.UsageRequested(cmd) {
		return usage.EmitSchema(cmd, cmd.OutOrStdout())
	}
	if interactive.ShouldPrompt(cmd, exchangeAuthCodeForAPIKeyCmdMeta) {
		if err := interactive.PromptAndSetFlags(cmd, exchangeAuthCodeForAPIKeyCmdMeta); err != nil {
			return err
		}
	}
	req, err := flagutil.BuildRequest[operations.ExchangeAuthCodeForAPIKeyRequest](cmd, exchangeAuthCodeForAPIKeyCmdMeta, "Body", "body")
	if err != nil {
		return err
	}
	s, err := client.NewClient(cmd)
	if err != nil {
		return err
	}
	sdkOpts, err := output.PrepareCallOpts(cmd)
	if err != nil {
		return err
	}
	// Dry-run: force skip deserialization so the synthetic empty response
	// does not cause parse failures in typed response handling.
	if client.IsDryRun(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	if err := output.ValidateGlobalServerName(cmd, sdk.ServerList); err != nil {
		return err
	}
	if output.WantsRawJSON(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	res, err := s.OAuth.ExchangeAuthCodeForAPIKey(cmd.Context(), *req, sdkOpts...)
	if err != nil {
		return output.Error(cmd, err)
	}

	if err := output.Result(cmd, res); err != nil {
		return err
	}
	return nil
}
